{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
<div class="min-h-screen bg-gradient-to-br from-emerald-50 to-teal-50 flex items-center justify-center p-4">
    <div class="bg-white rounded-2xl shadow-xl w-full max-w-md p-8 space-y-8">
        <div class="text-center">
            <h2 class="text-3xl font-bold text-gray-800 mb-2">Inscription</h2>
            <p class="text-gray-500">Rejoins la communauté !</p>
        </div>

        {% if app.user %}
            <div class="bg-emerald-100 border border-emerald-400 text-emerald-700 px-4 py-3 rounded-lg">
                Tu es déjà connecté en tant que {{ app.user.userIdentifier }}, 
                <a href="{{ path('app_logout') }}" class="font-medium text-emerald-600 hover:text-emerald-700">
                    Déconnexion
                </a>
            </div>
        {% endif %}

        {{ form_start(registrationForm, {'attr': {'class': 'space-y-6'}}) }}
            {% if form_errors(registrationForm) %}
                <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative">
                    {{ form_errors(registrationForm) }}
                </div>
            {% endif %}

            <div class="space-y-4">
                <div class="relative">
                    <svg xmlns="http://www.w3.org/2000/svg" class="absolute left-3 top-1/2 transform -translate-y-1/2 text-emerald-500 h-5 w-5" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
                    {{ form_widget(registrationForm.email, {
                        'attr': {
                            'class': 'w-full pl-12 pr-4 py-3 rounded-lg border border-gray-200 focus:border-emerald-500 focus:ring-2 focus:ring-emerald-200 outline-none transition-all',
                            'placeholder': 'Ton email'
                        }
                    }) }}
                </div>

                <div class="relative">
                    <svg xmlns="http://www.w3.org/2000/svg" class="absolute left-3 top-1/2 transform -translate-y-1/2 text-emerald-500 h-5 w-5" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="18" height="11" x="3" y="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
                    {{ form_widget(registrationForm.plainPassword, {
                        'attr': {
                            'class': 'w-full pl-12 pr-4 py-3 rounded-lg border border-gray-200 focus:border-emerald-500 focus:ring-2 focus:ring-emerald-200 outline-none transition-all',
                            'placeholder': 'Ton mot de passe'
                        }
                    }) }}
                </div>

                <div class="flex items-center space-x-2">
                    {{ form_widget(registrationForm.agreeTerms, {
                        'attr': {
                            'class': 'rounded border-gray-300 text-emerald-500 focus:ring-emerald-500'
                        }
                    }) }}
                    <span class="text-gray-600 text-sm">J'accepte les conditions d'utilisation</span>
                </div>
            </div>

            <button type="submit" class="w-full bg-emerald-500 text-white py-3 rounded-lg hover:bg-emerald-600 transition-colors flex items-center justify-center space-x-2 group">
                <span>S'inscrire</span>
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 group-hover:translate-x-1 transition-transform" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
            </button>
        {{ form_end(registrationForm) }}

        <p class="text-center text-gray-600 text-sm">
            Déjà inscrit ?
            <a href="{{ path('app_login') }}" class="text-emerald-500 hover:text-emerald-600 font-medium">
                Connecte-toi !
            </a>
        </p>
    </div>
</div>
{% endblock %}